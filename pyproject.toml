[project]
name = "django-spicy-id"
version = "1.1.0"
description = "Self-identifying, typed string IDs for Django models, including TypeID support."
readme = "README.md"
authors = [{ name = "mike wakerly", email = "opensource@hoho.com" }]
requires-python = ">=3.12,<4"
license = "MIT"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]

[dependency-groups]
dev = [
    "pytest>=8.3.2,<9",
    "tox>=4.17.1,<5",
    "setuptools>=72.1.0,<73",
    "pytest-django>=4.8.0,<5",
    "pydoc-markdown>=4.8,<5",
    "twine>=5.1.1,<6",
    "ruff>=0.5.7,<0.6",
    "django>=6.0.5",
]

[build-system]
requires = ["uv_build>=0.9.17,<0.10.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 100
target-version = 'py312'
exclude = [
  ".git",
  "node_modules",
  ".pyenv",
  ".vscode",
  ".tox"
]
